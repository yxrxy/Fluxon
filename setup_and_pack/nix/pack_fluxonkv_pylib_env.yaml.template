schema_version: 1

# Copy this file to pack_fluxonkv_pylib_env.yaml and adjust the local paths.
# The real pack_fluxonkv_pylib_env.yaml is gitignored on purpose.

store:
  project_data_root: /opt/example/fluxon-store-experiment

assembly:
  baseline_path: /opt/example/fluxon-store-experiment/manylinux-release

manylinux:
  runtime_image_ref: example-manylinux-builder:latest
  cargo_registry_dir: /opt/example/fluxon-store-experiment/manylinux-cache/cargo-registry
  cargo_git_dir: /opt/example/fluxon-store-experiment/manylinux-cache/cargo-git
